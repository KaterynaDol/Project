# 🎬 Итоговый проект: Movies Search

## 1. Общая идея проекта

Цель проекта — разработать Python-приложение для поиска фильмов
в базе данных **Sakila (MySQL)** с сохранением и анализом
пользовательских запросов в **MongoDB**.

Проект является итоговой работой сразу по двум модулям:
- Python
- Databases (MySQL + MongoDB)

Приложение реализовано в двух вариантах:
- консольное (CLI)
- веб-приложение (FastAPI)

---

## 2. Что реализовано в проекте

### 🔍 Функциональность

В проекте реализованы следующие возможности:

- поиск фильмов по ключевому слову (название)
- поиск фильмов по жанру и диапазону лет
- постраничный вывод результатов (pagination)
- логирование всех пользовательских запросов
- просмотр статистики запросов
- веб-интерфейс для поиска и статистики
- автоматические тесты

---

### 🗄️ Работа с базами данных

#### MySQL
- используется база данных **Sakila**
- выполняются SQL-запросы с `JOIN`, `WHERE`, `LIMIT`, `OFFSET`
- данные извлекаются через `mysql.connector`

#### MongoDB
- используется для хранения статистики запросов
- каждый запрос сохраняется как отдельный документ
- реализованы агрегатные запросы:
- Top 5 по частоте
- Last 5 уникальных запросов

---

### 🌐 Веб-интерфейс

Веб-приложение реализовано с использованием:
- **FastAPI**
- **Jinja2 Templates**

Веб-интерфейс включает:
- форму поиска
- страницу результатов
- страницу статистики

---

## 3. Архитектура проекта

Проект построен по модульному принципу:

- `main.py` — главное меню и точка входа
- `flows.py` — бизнес-логика (сценарии)
- `mysql_repo.py` — работа с MySQL
- `queries.py` — SQL-запросы
- `mongo.py` — логирование и статистика MongoDB
- `web_app.py` — FastAPI приложение
- `tests/` — автоматические тесты

Такое разделение позволило:
- избежать дублирования кода
- повысить читаемость
- упростить тестирование

---

## 4. Основные сложности и их решения

### ❗ Проблема 1: Смешение логики в одном файле

**Проблема:**
Изначально вся логика находилась в одном файле (`main.py`),
что делало код сложным для понимания и поддержки.

**Решение:**
Код был разделён на отдельные модули по зонам ответственности
(MySQL, MongoDB, бизнес-логика, интерфейс).

**Результат:**
Код стал структурированным, читаемым и удобным для тестирования.

---

### ❗ Проблема 2: Ошибки при работе с MongoDB

**Проблема:**
- статистика не обновлялась
- запросы не группировались корректно
- отображались неполные данные

**Решение:**
- пересмотр структуры документа MongoDB
- добавление чёткого формата хранения параметров
- корректная агрегация через `$group`, `$sort`, `$limit`

**Результат:**
Статистика отображается корректно и соответствует требованиям проекта.

---

### ❗ Проблема 3: Валидация пользовательского ввода

**Проблема:**
Пользователь мог вводить:
- некорректные годы
- диапазоны вне базы данных
- неверный формат

**Решение:**
- реализована отдельная функция парсинга диапазона лет
- добавлена проверка границ
- добавлены unit-тесты

**Результат:**
Приложение устойчиво к ошибкам ввода и не падает при некорректных данных.

---

### ❗ Проблема 4: Импорты и структура тестов

**Проблема:**
Тесты не находили модули проекта из-за структуры каталогов.

**Решение:**
- корректная организация проекта как Python-пакета
- правильные абсолютные импорты
- запуск тестов через `pytest` из корня проекта

**Результат:**
Все тесты успешно запускаются и проходят.

---

### ❗ Проблема 5: Smoke-тесты FastAPI

**Проблема:**
Smoke-тесты падали из-за неправильных импортов
и инициализации приложения.

**Решение:**
- исправлены импорты в `web_app.py`
- тесты адаптированы под FastAPI TestClient

**Результат:**
Smoke-тесты подтверждают, что веб-приложение запускается корректно.

---

## 5. Тестирование

В проекте реализованы:

- unit-тесты бизнес-логики
- тесты SQL-запросов
- тесты MongoDB агрегаций
- smoke-тесты веб-приложения

Тесты запускаются командой:

```bash
pytest


6. Итоговые выводы



В ходе работы над проектом:

получен практический опыт работы с MySQL и MongoDB

закреплены навыки модульной архитектуры Python

изучены основы FastAPI и шаблонов Jinja2

получен опыт написания автоматических тестов

улучшено понимание обработки ошибок и валидации данных



Проект полностью соответствует требованиям задания

и демонстрирует комплексный подход к разработке Python-приложений.

👩‍💻 Автор



Kateryna Dolinina

IT Career Hub

2026